---
interface Props {
	size?: "2xs" | "xs" | "small" | "large";
}

const { size } = Astro.props;

const height =
	size === "2xs"
		? "var(--space-xs)"
		: size === "xs"
			? "var(--space-m)"
			: size === "small"
				? "var(--space-l)"
				: size === "large"
					? "var(--space-3xl)"
					: "var(--space-2xl)";
---

<hr />
<div style={`height: ${height}`}></div>

<style>
  hr {
    border: 1px solid var(--color-caving);
  }
	div {
		display: block;
	}
</style>
