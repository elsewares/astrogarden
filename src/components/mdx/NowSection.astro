<section class="now-section">
	<slot />
</section>

<style>
	.now-section {
		border-left: 1px dashed var(--color-gray-300);
		padding-left: 2.8rem;
		margin-left: -1.2rem;
		margin-bottom: var(--space-3xl);
	}

	.now-section :global(h3) {
		margin-top: -3.4rem;
	}

	.now-section::before {
		content: "";
		width: 12px;
		height: 12px;
		border: 2px solid var(--color-gray-300);
		background: var(--color-cream);
		display: inline-block;
		margin-left: -3.2rem;
		margin-right: 1.7rem;
		position: relative;
		top: -1rem;
		border-radius: 50%;
	}

	.now-section :global(p:last-child) {
		margin-bottom: 0;
	}
</style>
